# weekly-review-dashboard — agent orientation

You are working inside `weekly-review-dashboard/`, the internal NA + EMEA/APAC BD/CS
weekly-review dashboard (React + Babel-standalone + Supabase, deployed at
na-rep-dashboard.netlify.app).

**Before editing anything here, read [README.md](README.md).** It is the single source of
truth for this app: the live file map, the no-module-system `window`-coupling architecture,
the load-bearing `<script>` order rule, the `data-model.js` / Supabase / RBAC model, the
build+deploy pipeline, the test suite, and step-by-step common tasks. It is kept accurate on
purpose so you don't have to relearn the codebase each time. `DEPLOY.md` holds the deploy +
team-admin seating runbook.

This file only augments the repo-root persona dispatcher — it does not change who you are.
