[build]
  command = "python3 build/bundle.py"
  publish = "dist"

[build.environment]
  PYTHON_VERSION = "3.11"

# Single-file SPA — every path serves the same index.html.
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
